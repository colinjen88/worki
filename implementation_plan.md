# WANG 作品集 v3.1 最終修正版計畫

版本：v3.1 審核定稿｜修訂日期：2026-09-06  
目標：保留舊站的真實內容、踏實語氣與動效特色，沿用已完成的 Next.js 架構，完成可閱讀、可操作、可核實的現代淺色作品集。  
本次交付：計畫書審核與修訂；下列網站修正與驗收均為後續工作，尚未執行。  
文件主本：專案根目錄 `implementation_plan.md`；取代原 v3.1 提案。後續變更以此檔案納入版本管理。

## 1. 審核結論與實際基準

方向成立，但原稿不足以直接作為驗收依據：需修正樣式工具假設、區分已完成功能與待辦、建立內容證據規則，並補足互動、靜態路由與發布條件。

| 項目 | 本次確認的現況 | 定稿決策 |
|---|---|---|
| 開發基準 | 目前分支為 `feat/portfolio-v3`，HEAD 為 `4582432`；審核開始時工作目錄乾淨 | 沿用此分支，不從 `master` 重建、不覆蓋既有成果；實作前重新記錄 HEAD 與工作目錄狀態 |
| 技術架構 | 已安裝 Next.js 16.3.4；宣告依賴為 React 19、TypeScript；已有首頁、資料層及四個案例 slug | 本次為內容校準、淺色化與品質修正；套件以 `package-lock.json` 為準，不為改色升級框架 |
| 樣式系統 | `globals.css` 手寫工具類別；存在舊 `tailwind.config.js`，但沒有 `tailwind.config.ts`，目前依賴與建置未接入 Tailwind | 沿用 CSS 變數與 CSS Modules／語意類別，逐元件替換；不得假設新增 Tailwind class 會自動產生樣式 |
| 靜態輸出 | `next.config.mjs` 已設定 `output: 'export'`、`trailingSlash: true`、`images.unoptimized: true` | 保留設定；輸出與部署單位為完整 `out/` |
| 個人程式碼卡片 | 資料位於 `personalInfo.designerConfigCode`，不是獨立 `designer.config.ts` 設定檔 | 保留視覺概念；展示資料與真實設定分清，移除不存在的 `designerProfile.init()` 呼叫 |
| 精選案例與圖集 | 現有首頁是一個可篩選作品庫；卡片有輪播，案例內頁是直列圖集 | 精選區是新增工作；內頁保留易閱讀圖集，不把「測試內頁輪播」列為既有功能 |
| 原研究 | `docs/portfolio-redesign-plan-2026-09-05.md` | 沿用內容證據、工程品質與可回復發布的原則；本定稿優先決定 v3.1 範圍 |
| 線上來源 | 本次透過 HTTP 取得主站 HTML，看到植園、御手、GoodEats、GoldLab 與報價作品；本機有 `let/index.html` | 可作內容對照，不能證明線上版本與某個 Git 提交完全一致，也不能證明作者職責與成果 |

原稿來源：`C:/Users/colin/.gemini/antigravity-ide/brain/7c1fe27e-22e2-41ea-8913-269d1c599e6b/implementation_plan.md`。原位置同步保留本次定稿；舊稿另行備份。

## 2. 產品定位與範圍

以「前端工程師／前端設計師」為主要職務表述，呈現 UI/UX、RWD、動效與網站實作能力。首頁讓招聘方快速找到定位、代表作品、實際負責內容及聯絡方式，不自行增加資深頭銜、框架年資、求職狀態或未確認的工作條件。

**本版必要交付：**

- 全站單一淺色主題，包含首頁、案例頁、404、導覽、互動狀態與彈窗。
- 舊站文案及完整作品入口盤點，四個案例頁完成證據校準。
- 精選案例摘要區、完整作品篩選、卡片圖像切換、影片彈窗與聯絡功能。
- 修正現有功能缺陷，完成靜態輸出、鍵盤操作、手機版與基本 SEO 驗收。
- 產出預覽及驗收紀錄；正式發布另按第 9 節執行。

**後續可選增強：**卡片翻轉、Lottie 裝飾、打字機、游標互動。只在內容及效能驗收完成後評估；不為保留「精神」而一次引入所有動畫。

本版不新增深色切換、CMS、登入、聯絡表單後端、全站多語系或新工程示範專案，也不重寫被展示的客戶網站。先讓現有案例與程式碼形成可信證據。

## 3. 內容校準與真實性規則

### 3.1 來源與發布判準

建立 `docs/portfolio-content-audit.md`，逐項記錄：專案 ID、原文／原始 URL 或檔案位置、擬發布文案、本人角色、作品性質、圖片說明、核對日期、證據及核對狀態。狀態區分「已核對」「待本人確認」「移除或改寫」。

- 舊站原文是內容基準，不是自動驗證。職責、合作方式、後台功能、業務結果與 SEO 成效需有本人確認或適當證據。
- 不從截圖推論資料串接完成、不從公開網站可開啟推論全部由本人開發；不以「100% 真實專案」徽章替代說明。
- 深度敘述只寫可支持的背景、角色、限制、決策與交付。無量測資料時描述交付物，不宣稱提高轉換、降低成本或取得排名。
- `projects.ts` 現有 `details` 不是已驗證來源。例如御手「排班與雙向對接」、植園「品牌規劃／全站開發」、GoldLab「SEO 結構資料」、圖片對應的後台功能，均應逐項核實。
- 原稿所稱「備受肯定」「最佳實踐」缺少可追溯依據，改為設計方向；保留原語氣不代表保留誇大敘述。
- 尚未確認的段落不對外發布；可先完成頁面模板並使用已確認的簡版內容。必填資訊至少包含作品名稱、性質、本人工作範圍、可展示的交付物及入口。
- 不自行補上「目前開放工作機會」。狀態未確認時只顯示職務定位。個人介紹、Why Me、metadata 與程式碼卡片用詞一致。

### 3.2 作品與入口對照

下表 URL 是現有資料的對照值；本次未逐站驗證作品運作，發布前須檢查導向及可用性。

| 作品 | v3.1 路徑／呈現 | 原始入口與注意事項 |
|---|---|---|
| 植園花室 Zhiyuan Floral Studio | `/work/zyfloral/`，精選 | `https://zyfloral.com/`；核實官網／後台的本人工作範圍 |
| 御手國醫－前／後台試做 | `/work/royal/`，精選 | `https://royal.gowork.run/showcase`；保留「試做」，連結標為展示／開發介紹，不暗示正式委託或商用上線 |
| 穀意 GoodEats | `/work/goodeats/`，精選 | 官網 `https://goodeats.asia/` 與開發介紹 `https://gowork.run/showGoodEats/` 分開標示；可整併成一個作品但兩個入口都保留 |
| 內容型網站完整建置（GoldLab） | `/work/goldlab/`，精選 | `https://goldlab.tw/`；不合併報價網站的功能或成果 |
| 聚合免費報價來源（資料 ID：`flashrate`） | 完整作品庫獨立卡片，本版不新增案例路由 | `https://goldlab.cloud/`；「15 個異構數據源」需核對，未確認不強調數字 |
| 其他作品 | 完整作品庫 | 保留 `shiny-jewelry`、`alloy-team`、`motion-collection`、`graphic-portfolio`、`seo-strategy`、`micro-interactions`，逐筆對照舊站及現有資料 |

目前資料共 11 筆，作為盤點起點。以舊站全部作品入口建立映射，說明合併／保留／移除理由，不靠筆數相同宣稱完整。整理中作品以狀態文字呈現，不放假連結或無反應按鈕。

### 3.3 資料層調整

沿用 `src/data/projects.ts`，按需要小幅補充作品性質、證據連結與內容核對狀態；實作時決定最小欄位集，不預先設計大型 CMS schema。案例頁與精選卡片從同一資料來源讀取。

- `id` 與 `slug` 各自唯一；路由只使用可發布且具備必要內容的案例。
- 「全部」是篩選狀態，不是作品類別：將 `all` 從作品分類型別拆出，保留在篩選值型別中。
- 圖片及 PDF 的網站路徑 `/assets/...` 對應 `public/assets/...`；根目錄 `assets/` 或 `let/assets/` 不會自行成為新版資產。
- 圖片說明必須對應實際畫面；不存在或未確認的說明不得憑圖片順序編造。多圖可以沿用陣列，但需檢查說明數量及順序，或改用圖像物件避免錯配。

## 4. 資訊架構與閱讀路徑

```text
首頁 /
├── Navbar：Logo／關於我／核心職能／作品／個人特點／聯絡
├── Hero #about：職務定位、原站精簡介紹、查看作品與聯絡 CTA
├── CoreExpertise #expertise：UI/UX 與 RWD、Motion、前端實作
├── Featured Case Studies #featured：四個精選案例的短摘要與閱讀入口
├── WorksSection #works：完整作品庫與類別篩選（初始為全部）
├── WhyMe #whyme：舊站三項協作特質、個人程式碼視覺卡片
└── Footer #contact：可選取 Email、mailto、複製、LINE

案例 /work/{zyfloral|royal|goodeats|goldlab}/
├── 標題、作品性質、簡介、本人角色、官網／展示入口
├── 已核對的問題、限制、做法、交付成果
├── 圖集與逐圖說明
└── 回作品庫與聯絡入口

404：淺色錯誤頁、回首頁與作品庫
```

精選區用簡潔摘要避免與完整作品卡片重複大段文字和輪播。首頁主 CTA 指向精選區，導覽「作品」維持 `/#works`，保留舊錨點。所有共用導覽由內頁也能回首頁對應區段；固定導覽遮蔽問題使用適當的捲動留白處理。

案例頁首屏先交代角色與入口，不要求訪客滑完多張大圖才看到重點。圖集預設直列、不自動輪播，必要時允許開啟原圖閱讀細節。

## 5. 淺色設計系統

風格為清楚留白、細緻邊框、靛藍重點色與少量紫粉裝飾。優先保留 WANG 品牌與作品本身的辨識度，避免全部套用相同 SaaS 卡片。

| 語意 token | 初始值／原則 |
|---|---|
| `--surface-page` | `#f8fafc` |
| `--surface-card` | `#ffffff` |
| `--text-primary` | `#0f172a` |
| `--text-secondary` | `#475569` |
| `--brand` | `#4f46e5`；實心主按鈕搭配白字 |
| `--border-subtle` | `#e2e8f0`，用於裝飾分隔；必要控制項邊界另以對比驗收 |
| `--focus-ring` | `#4338ca`，清楚可見並留間距 |
| 陰影／毛玻璃 | 低透明度陰影；毛玻璃限導覽等少量區塊，有不透明退化背景 |

以上為起始設計值，實際疊色、hover、disabled、focus 均須檢查。正文不使用淺紫粉漸層字。全站設定 `color-scheme: light`；舊作品截圖及影片本身可維持原色，避免反相濾鏡。

以 `globals.css` 管理基礎 token、字體、重設與共用規則；元件複雜樣式使用 CSS Modules 或有範圍的語意類別。逐一遷移手寫工具類別及硬編碼顏色，不全域把 `.text-white` 改成黑字，以免破壞實心按鈕和影片控制。清理不再使用的規則，不新增第二套樣式生成流程。

正文以 16px 為起點，中文行高約 1.6–1.8；窄螢幕單欄、寬螢幕適度增加欄數。長 Email、URL 及程式碼不撐破版面。字體採可靠系統字體作預設；若新增自託管字體，需確認授權、檔案大小與 fallback。

## 6. 元件與互動必要修正

| 元件／位置 | 實作要求與通過條件 |
|---|---|
| `Navbar.tsx` | 共用連結改為 `/#about` 等完整首頁錨點；手機選單具 `aria-expanded`、控制目標、Escape 關閉及合理焦點行為，點擊入口後收合 |
| `Hero.tsx`、`CoreExpertise.tsx`、`WhyMe.tsx` | 依內容對照表還原精簡原語氣；動效不延遲主標與 CTA；程式碼卡片是可讀的個人資料展示，不暗示可執行程式 |
| 新精選區／`WorksSection.tsx` | 以同一資料集產生精選與完整庫；篩選按鈕呈現選取狀態及結果數，支援鍵盤；零筆有明確文字 |
| 卡片圖像切換 | 真正的按鈕具可辨識名稱、目前圖數；一張圖不顯示切換；不自動播放。使用 link／button 提供實際可操作入口，不以只有 click handler 的 div 代替，也不巢狀互動元素 |
| 案例圖集 | 保留完整 UI 畫面與原始比例，必要時 `contain`／開原圖；避免用固定 `object-cover` 裁掉功能證據；提供尺寸、替代文字、說明與載入失敗替代入口 |
| `VideoModal.tsx` | 修正現有 `replace(...).split('?')[0]` 導致 YouTube 備援網址遺失影片 ID 的錯誤；用明確的 watch URL／影片 ID 建立播放入口。加入對話框名稱、開啟焦點、焦點限制、背景不可操作、Escape、關閉後焦點還原與停止播放；備援連結不遮擋播放器控制 |
| `Footer.tsx` | 目前複製失敗仍顯示成功，必須修正。成功才顯示「已複製」；失敗提示手動複製並保留可選取文字及 mailto；狀態可由輔助科技讀取 |
| 動效 | 保留少量 hover、轉場及捲動回饋；`prefers-reduced-motion` 時停用非必要 CSS 與 JavaScript 動態。首屏不加阻擋式 Lottie loading；翻轉卡片不隱藏重要資訊 |

## 7. Next.js、資產與 SEO 約束

1. 實作前遵守 `AGENTS.md`，閱讀目前安裝版本 `node_modules/next/dist/docs/` 的相關文件，尤其 Static Exports、Server and Client Components、`generateStaticParams` 與 Image；不套用舊版本 API。
2. 保留案例的 `generateStaticParams()`、`dynamicParams = false` 與 `await params`。四個已發布案例須在建置時生成；未知 slug 由靜態主機回傳真正的 HTTP 404。
3. 首頁目前整頁為 Client Component。將捲動進度、作品篩選與影片狀態收斂到必要互動元件，讓可靜態呈現的內容維持 Server Component；不跨 server/client 邊界傳遞一般事件函式。停用 JavaScript 時仍可讀主要介紹、作品與聯絡入口。
4. 不引入需要執行期伺服器的 Server Actions、登入 cookies、ISR 或表單 API。靜態輸出使用 `next build`；不使用已移除的 `next export` 或以 `next start` 預覽靜態產物。轉址與回應標頭在實際託管層處理。
5. `images.unoptimized: true` 不會自動壓縮或產生響應式圖片。事先準備合適尺寸與 WebP 等資產；只有指定 `sizes` 不等於已減少傳輸量。首頁不預載全部作品圖；案例首張／實際 LCP 圖優先，其餘延遲載入。
6. `layout.tsx`、首頁及案例使用一致職務定位；每頁有唯一 title、description、canonical 與合適的 Open Graph／Twitter 資料，避免標題模板重複 WANG。分享圖的實際尺寸與宣告一致，案例分享資訊不意外沿用首頁內容。
7. 本版以 `public/robots.txt`、`public/sitemap.xml` 產生靜態 SEO 檔；sitemap 僅列已發布頁面，新增／移除 slug 時同步更新。主站 URL 使用 `https://let.gowork.run/`；預覽環境避免被索引，發布時確認正式站沒有殘留 noindex。

技術依據：[Next.js 靜態輸出官方文件](https://nextjs.org/docs/app/guides/static-exports)；實際程式 API 仍以本機安裝版本文件為準。靜態輸出可降低執行期需求，但效能必須量測，不保證「載入極速」。

## 8. 執行順序與完成定義

| 階段 | 工作及交付 | 進入下一階段條件 |
|---|---|---|
| A 基準與內容 | 記錄分支、提交、Node/npm 版本及既有 typecheck/build 結果；完成內容／資產／舊網址對照表 | 未證實敘述有明確處理，四案例必填資料可用；既有建置問題有紀錄與處理方案 |
| B 設計基礎 | token、首頁首屏、一張作品卡及案例首屏先完成淺色版本，保存桌面與手機截圖 | 實際 CSS 生效、品牌與字級一致，對比與窄版可讀；不得只做字串換色 |
| C 頁面與資料 | 全站套用、精選區、完整庫、四案例、404、SEO 與資產整理 | 路由／入口與資料表一致，沒有未核對成果或遺漏作品入口 |
| D 互動與品質 | 完成第 6 節修正、靜態預覽及下方測試 | 必要檢查全部通過，缺陷已修正；可選動畫不阻擋交付 |
| E 發布準備 | 建立預覽、`docs/portfolio-v3.1-qa.md`、部署與回復說明 | 可定位發布產物、版本、測試證據及回復目標；滿足第 9 節發布條件 |

### 驗收清單

- **建置**：乾淨安裝環境依 lockfile 執行 `npm ci`，再依序 `npm run typecheck`、`npm run build`，皆 exit 0。預覽完整 `out/`，可用既有 `npm run start`（目前為 `serve out`），不只檢查 dev server。不要使用 SPA fallback 掩蓋 404。
- **輸出／路由**：`out/index.html`、四個 `out/work/<slug>/index.html`、`out/404.html`、robots 與 sitemap 存在。逐頁直接開啟、重新整理、站內跳轉及返回正常；未知路徑回 404，所有本機資產與 PDF 回 200 且內容類型正確。
- **內容**：四案例無未核實角色與效果聲明；御手標示試做；GoodEats 雙入口可辨識；GoldLab 與報價分列。外站失效記錄日期並提供可用替代或移除 CTA，不將擷取失敗當成網站永久下線。
- **核心流程**：每類篩選、零筆結果、每組多圖切換、單圖狀態、影片開／關及 YouTube 備援、Email 複製成功／拒絕、mailto、LINE、手機選單與跨頁錨點均手動驗證。
- **必要回歸測試**：對 YouTube URL 轉換、複製拒絕回饋及 modal 焦點流程保留小型自動化回歸；可用瀏覽器測試覆蓋，不為純配色寫大量測試。實作時加入最小工具與可重跑命令，不能在尚未設定測試工具時宣稱已有測試通過。
- **版面**：至少檢查 360、390、768、1280、1440px，另確認 320px 寬的內容回流及 200% 縮放；無整頁橫向溢出、文字截斷或導覽遮擋。記錄 Chrome／Edge 版本，並以實際 iOS Safari 檢查觸控；模擬測試不能標成實機通過，未完成列為待驗收。
- **無障礙**：以 WCAG 2.2 AA 為目標：一般文字對比至少 4.5:1、大字至少 3:1，必要非文字控制至少 3:1；操作目標原則至少 24×24 CSS px（遵守規範例外），本案主要按鈕採 44px 高。鍵盤可完成全部核心流程、focus 可見、skip link 有效、modal 不困住使用者，減少動態模式仍可使用。自動掃描加鍵盤與螢幕閱讀器抽查，不以掃描零錯誤宣稱全面符合。[WCAG 2.2 檢核參考](https://www.w3.org/WAI/WCAG22/quickref/)
- **效能**：以 production 靜態預覽的首頁及圖片最重案例各跑三次 Lighthouse mobile，固定版本／設定、冷快取、記錄中位數；本版目標 Performance ≥90、LCP ≤2.5s、CLS ≤0.1，未達時先減圖／字體／動畫後重測。這些是專案實驗室驗收目標，不是現站成績，也不等同真實使用者 Core Web Vitals 通過。
- **證據**：QA 文件附測試命令、結果、截圖、環境、版本、未完成項及缺陷處理；不勾選未執行項。本次計畫審核未執行網站 build、視覺、效能或無障礙測試。

## 9. 發布與回復計畫

本次只修訂文件，不執行部署。後續實作完成後，先發布隔離預覽供檢視，正式站切換在發布工作範圍內執行。

- 現有 `let/deploy_vps_let.ps1` 拉取 `master` 並將網站根目錄指向 `let/`，另會安裝／配置 Nginx、Certbot；它不會部署這份 Next.js `out/`，不可直接拿來發布 v3.1。
- 本次讀取 `.antigravity` 指定的 `https://let.gowork.run/jen/api/agent-context` 得到 404 HTML，未取得可用規則。因此主機路由、容器、port 及註冊介面仍待確認；不根據舊文件或單一回應標頭推定實際部署拓撲。此項只影響正式發布準備，不阻擋內容與前端修正。
- 發布前取得有效部署規則及現站設定，確認既有專案路由（包含共用網域下其他服務）不受影響。若沿用 Gateway 管理容器，依有效規則配置並於成功後完成要求的專案註冊；不修改 Gateway 核心程式。
- 使用專案專屬發布流程：以確定提交建置，將完整 `out/` 放入新的版本目錄，檢查首頁、內頁、資產、404、HTTPS 及分享 metadata 後再切換。保留前一版可用產物與設定，避免先刪除現站或混用不同版本的 HTML／JS。
- 舊 URL 依對照表保留必要靜態頁或在託管層做精確 301；不可把所有未知路徑導回首頁。`/assets/` 的既有被引用資源須兼容或有明確遷移方案。
- 發布後重跑 HTTP 冒煙及核心入口檢查，確認 CDN 沒有混用新舊資產。出現首頁／案例不可用、廣泛資產 404 或主要聯絡入口失效時，切回已驗證上一版並重驗；QA 紀錄發布版本與回復結果。

## 10. 本次定稿的關鍵修正

1. 以已存在的 v3 分支及元件為基準，移除重建分支、直接調整不存在 Tailwind 設定的步驟。
2. 把真實內容要求落成逐項證據與發布規則，保留試做性質及原有完整作品入口。
3. GoldLab 與報價作品分開，GoodEats 的官網與開發介紹保留雙入口；內頁圖集與首頁輪播分清。
4. 以語意化 CSS token 與實際元件遷移定義淺色化，補上導覽、影片、複製、焦點及動效退化的必要修正。
5. 明確列出靜態輸出、資產、SEO、品質門檻、發布限制與回復條件，移除原稿泛用的「Proceed／繼續」確認段落。

本計畫可作為後續實作基準；內容待核實項及部署待確認項有具體處理方式，不應被誤標為已完成驗收。
